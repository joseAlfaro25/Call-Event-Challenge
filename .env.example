# Root directory of the challenge input contract. Relative paths use the project root.
KONTAKTU_BASE=./reto-kontaktu

# Safe deterministic mode for local replay; set to 0 for live LLM classification.
OFFLINE_REPLAY=1
MODEL=gpt-5.6-luna

# Required only when OFFLINE_REPLAY=0.
# OPENAI_API_KEY=sk-your-key-here
